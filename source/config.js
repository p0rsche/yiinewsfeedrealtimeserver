//uncomment neccessary string to tune up server; don't forget that salt string should also be changed on server-side
var config = {
  debug: true,
  log_level: 3
  //history_count: 5,
  //host: 'http://localhost',
  //port: 2206,
  //salt: 'th!3!3SpARTa!'
}

module.exports = config;